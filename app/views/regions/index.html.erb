<h1>Regions#index</h1>
<div class = "row">
  <% @regions.each do |region| %>
    <div class="col-md-4">
      <%= link_to (image_tag region.image) %>
      <p><%= link_to region.name %></p>
    </div>
  <% end %>
</div>
